"use strict";
/**
 * Метод "animate" выполняет анимацию над элементом, на котором был вызван
 */
Object.prototype.animate=function(d,f,b){
  function m(a){
    switch(b.easingFunc){
      case "linear":return a;
      case "easeInQuad":return Math.pow(a, 2);
      case "easeOutQuad":return 1 - Math.pow(1 - a, 2);
      case "easeInOutQuad":return .5 > a ? Math.pow(2 * a, 2) / 2 : (2 - Math.pow(2 * (1 - a), 2)) / 2;
      case "easeInCubic":return Math.pow(a, 3);
      case "easeOutCubic":return 1 - Math.pow(1 - a, 3);
      case "easeInOutCubic":return .5 > a ? Math.pow(2 * a, 3) / 2 : (2 - Math.pow(2 * (1 - a), 3)) / 2;
      case "easeInQuint":return Math.pow(a, 5);
      case "easeOutQuint":return 1 - Math.pow(1 - a, 5);
      case "easeInOutQuint":return .5 > a ? Math.pow(2 * a, 5) / 2 : (2 - Math.pow(2 * (1 - a), 5)) / 2;
    }
    return b.easingFunc(a);
  }
function h(a){a=(a-n)/b.duration;1<a&&(a=1);var c=m(a),e;for(e in d){d.hasOwnProperty(e)&&(k.style[e]=g[e]+c*l[e]+"px");}1>a?requestAnimationFrame(h):f&&f();}f=f||null;b=b||{};b.easingFunc=b.easingFunc||"linear";b.duration=b.duration||1000;var k=this,p=getComputedStyle(k),g={},l={},c;for(c in d){d.hasOwnProperty(c)&&(g[c]=parseFloat(p[c]),l[c]=parseFloat(d[c])-g[c]);}var n=performance.now();requestAnimationFrame(h);};