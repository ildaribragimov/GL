"use strict";
Object.prototype.animate=function(a,b,c){b=b||null;с=с||new Object;c.easingFunc=c.easingFunc||'linear';c.duration=c.duration||1000;var d=this,e=getComputedStyle(d),f=new Object,g=new Object;for(var j in a){if(a.hasOwnProperty(j)){f[j]=parseFloat(e[j]);g[j]=(parseFloat(a[j])-f[j]);}}
function q(n){switch (c.easingFunc){
    case 'linear':return n;
    case 'easeInQuad':return Math.pow(n,2);
    case 'easeOutQuad':return 1-Math.pow(1-n,2);
    case 'easeInOutQuad':return (n<0.5)?(Math.pow(2*n,2)/2):((2-Math.pow(2*(1-n),2))/2);
    case 'easeInCubic':return Math.pow(n,3);
    case 'easeOutCubic':return 1-Math.pow(1-n,3);
    case 'easeInOutCubic':return (n<0.5)?(Math.pow(2*n,3)/2):((2-Math.pow(2*(1-n),3))/2);
    case 'easeInQuint':return Math.pow(n,5);
    case 'easeOutQuint':return 1-Math.pow(1-n,5);
    case 'easeInOutQuint':return (n<0.5)?(Math.pow(2*n,5)/2):((2-Math.pow(2*(1-n),5))/2);
}return c.easingFunc(n);}
function o(n){var p=q(n);for(var j in a){if(a.hasOwnProperty(j)){d.style[j]=f[j]+p*g[j]+"px";}}}
function m(time){var n=(time-k)/c.duration;if(n>1){n=1;}o(n);if(n<1){requestAnimationFrame(m);return;}if(b){b();}}
var k=performance.now(),l=requestAnimationFrame(m);}